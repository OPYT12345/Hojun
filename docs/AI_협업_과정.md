# AI 협업 과정 문서

**프로젝트명:** 다기능 수업 비서 프로그램  
**협업 AI:** Claude (Anthropic)  
**협업 기간:** 2026년 3월 ~ 4월  

---

## 1. 협업 방식 개요

본 프로젝트는 **기획자(인간) + 구현 파트너(AI)** 모델로 개발되었다.

- **기획자 역할 (개발자):** 문제 정의, 기능 아이디어 제안, 결과물 검토, 테스트 및 피드백
- **AI 역할 (Claude):** 아키텍처 설계, 코드 구현, 보안 검토, 버그 수정, 최적화 제안

AI는 단순한 코드 생성 도구가 아니라, 설계 단계부터 함께 참여하는 **협업 파트너**로 활용하였다.

---

## 2. 단계별 협업 내역

### Phase 1 — 기반 구조 설계
**기획자 요청:** 로그인/회원가입 시스템, 강사/학생 역할 분리, HTTPS 적용  
**AI 기여:**
- Spring Boot + JPA + MySQL 프로젝트 구조 설계
- BCrypt 비밀번호 암호화, 세션 기반 인증 구현
- HTTPS keystore 설정 방법 안내 및 적용
- 로그인 실패 횟수 제한(LoginAttemptService) 설계

---

### Phase 2 — NFC 출석 시스템
**기획자 요청:** NFC 태그 하나로 출석 처리가 되면 좋겠다  
**AI 기여:**
- `/nfc/{lectureId}` 단축 URL 설계 제안
- `NfcRedirectController` 구현
- 미로그인 상태 접근 처리 로직 설계
- 모바일 접속을 위한 방화벽 오픈 스크립트(`open-firewall.bat`) 제공

---

### Phase 3 — 과제 시스템 설계
**기획자 요청:** 문제 방식보다 자유 서술 과제 방식이 더 적합할 것 같다  
**AI 기여:**
- `Assignment` 엔티티 재설계 (기간 설정, 첨부자료 JSON 구조)
- `Submission` 엔티티 설계 (텍스트 + 파일/링크 복합 제출)
- D-day 카운트다운, 기한 초과 처리 로직
- 강사 제출 현황 15초 자동 갱신 구현

**협업 포인트:** "Problem → Assignment 전환"이라는 기획 변경을 AI가 기존 코드와의 충돌 없이 안전하게 리팩토링

---

### Phase 4 — AI 튜터 / 분석 기능
**기획자 요청:** 학생용 AI 튜터와 강사용 분석 기능을 넣고 싶다  
**AI 기여:**
- OpenAI GPT-4o-mini API 연동 설계
- `AiService` 구현 (학생 튜터, 강사 분석, Q&A 답변 제안)
- 환경변수 기반 API 키 관리 방법 설계 (보안)
- 토큰 낭비 방지를 위한 프롬프트 최적화 및 사용량 한도(`AiBudgetService`) 설계

---

### Phase 5 — 채점 및 알림 시스템
**기획자 요청:** 채점 결과를 학생이 바로 알 수 있게 해줘  
**AI 기여:**
- `Notification` 엔티티 및 인앱 알림 시스템 전체 설계
- 알림 유형 3종(GRADE / QA_ANSWER / BROADCAST) 정의
- 벨 아이콘 UI, 미읽음 배지, 모달 목록 구현
- `@PreUpdate` 충돌 버그 발견 및 수정

---

### Phase 6 — 공지 방송 시스템
**기획자 요청:** 강사가 전체 수강생에게 공지를 보낼 수 있으면 좋겠어  
**AI 기여:**
- `Announcement` 엔티티 설계
- 수강생 전원 동시 알림 발송 로직 (N+1 방지 쿼리 포함)
- 강사/학생 양측 UI 탭 추가
- 알림에 강의명 표시 (채점·Q&A·공지 전 유형 통일)

---

### Phase 7 — Docker 배포 환경
**기획자 요청:** 심사관이 한 번에 실행할 수 있게 해줘  
**AI 기여:**
- 멀티스테이지 `Dockerfile` 설계 (Maven 빌드 → JRE 실행)
- `docker-compose.yml` 설계 (MySQL + Spring Boot 통합)
- `data.sql` 샘플 데이터 자동 삽입 구조
- `start.bat` 로컬 실행 스크립트

---

### Phase 8 — 보안 검토
**AI 주도적 기여 (기획자 요청 없이 발견):**
- SSE 엔드포인트 강사 인증 누락 → 수정
- 파일 업로드 경로 순회(Path Traversal) 취약점 → 수정
- 강의 소유권 미검증 → 수정
- 잘못된 입력값에 500 반환 → 400으로 수정
- `Content-Disposition` 헤더 인젝션 취약점 → 수정

---

### Phase 9 — 학생 관리 탭 및 출석 이력 버그 수정
**기획자 요청:** 강사가 수강생별 출석률과 과제 제출 현황을 한눈에 볼 수 있으면 좋겠어  
**AI 기여:**
- `teacher-manage.html` 에 "👥 학생 관리" 탭 추가
- 수강생 카드 UI: 이름·학번·이메일·자리번호, 출석률 색상 코딩 (80%↑ 녹색 / 60~79% 주황 / 60%↓ 빨강)
- 카드 클릭 시 날짜별 출석 이력 펼치기
- `GET /api/teacher/lectures/{id}/students` — 요약 엔드포인트
- `GET /api/teacher/lectures/{id}/students/{sid}/attendance` — 상세 이력 엔드포인트

**버그 수정 (AI 주도적 발견):**
- `lectureEnd`가 미래일 때 그 이후 날짜 전부 "결석" 표시 → `min(lectureEnd, 오늘)`로 수정
- `lectureStart` 미설정 시 빈 목록 반환 → 실제 출석 기록 우선 조회 후 fallback 처리
- 스케줄 외 요일 출석 기록 누락 → 비정규 출석도 이력에 포함

---

### Phase 10 — 라이트 / 다크 테마
**기획자 요청:** 밝은 테마도 넣고 싶어  
**AI 기여:**
- 전체 13개 HTML 파일에 라이트/다크 테마 CSS 변수 적용
- `<body>` 직후 early 스크립트로 FOUC(깜빡임) 없이 즉시 테마 적용
- `localStorage` 기반 테마 영속 저장 (페이지 이동해도 유지)
- `[ 🌙 다크  ☀️ 라이트 ]` 세그먼트 토글 버튼 UI

---

### Phase 11 — 비밀번호 변경 기능
**기획자 요청:** 로그인된 상태에서 비밀번호를 바꿀 수 있게 해줘  
**AI 기여:**
- `PUT /api/user/password` 엔드포인트 구현 (`UserProfileController`)
- 현재 비밀번호 확인 후 변경, 8자 이상 검증, BCrypt 해시 저장
- 학생/강사 강의 목록 페이지 헤더에 "비밀번호 수정" 버튼 + 모달 추가

---

### Phase 12 — Railway 클라우드 배포
**기획자 요청:** 심사관이 직접 설치 없이 접속할 수 있는 라이브 URL이 필요해  
**AI 기여:**
- Railway 환경변수 체계로 `application.properties` 재구성
  - DB: `MYSQLHOST`, `MYSQLPORT`, `MYSQLUSER`, `MYSQLPASSWORD`, `MYSQLDATABASE`
  - SSL: `${SSL_ENABLED:true}` 토글 (Railway에서는 `false`)
  - 쿠키: `${COOKIE_SECURE:true}` 토글 (Railway에서는 `false`)
- 배포 후 라이브 URL 확보: `https://hojun-production-6399.up.railway.app`

---

## 3. AI 협업의 핵심 원칙

### 3-1. 기획자가 방향을 결정, AI가 구현을 담당
모든 기능의 **존재 여부는 기획자가 결정**하였다. AI는 "어떻게 구현할 것인가"를 담당하되, 기획자의 승인 없이 임의로 기능을 추가하지 않았다.

### 3-2. AI의 제안을 기획자가 검토
AI가 구조나 방식을 제안할 때, 기획자가 검토 후 채택 여부를 결정하였다. 예: 도메인 주도 패키지 구조 전환은 AI가 제안했지만 현재 규모에서는 불필요하다고 판단하여 보류.

### 3-3. 테스트는 기획자가 직접
기능 구현 후 실제 브라우저/모바일에서의 테스트는 기획자가 직접 수행하고, 발견된 버그를 AI에게 전달하여 수정하였다.

### 3-4. 보안 및 안정성은 AI가 주도적으로 검토
배포 전 코드 검토 단계에서 AI가 실제 운영 환경에서 문제가 될 수 있는 보안 취약점을 능동적으로 발견하고 수정하였다.

---

## 4. 사용한 AI 도구

| 도구 | 용도 |
|------|------|
| Claude (Anthropic) | 전체 설계, 코드 구현, 보안 검토, 문서 작성 |
| OpenAI GPT-4o-mini | 서비스 내 AI 튜터 및 분석 기능 (런타임) |
